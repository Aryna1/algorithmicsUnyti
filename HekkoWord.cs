using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HekkoWord : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //imprimimos la entrada del game 
        print("Hola :)");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
